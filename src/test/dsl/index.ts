/**
 * DSL (Domain Specific Language) exports for testing
 *
 * Usage:
 * - Import BaseDsl to create new DSL classes
 * - Each usecase should have its own DSL class extending BaseDsl
 * - DSL provides fluent API for Given-When-Then pattern
 */

export { BaseDsl } from "./BaseDsl";
